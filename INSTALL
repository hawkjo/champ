# Install system packages.

sudo apt install \
    sextractor \
    samtools \
    bowtie2 \
    gcc \
    gfortran

# Install Python packages. The scipy installer goes ahead even if its dependencies aren't met so
# we have to do this in a three-step process.

pip install numpy && pip install scipy && pip install -r requirements.txt
